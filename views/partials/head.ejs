<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= site?.title || 'My Portfolio' %></title>
  <link rel="stylesheet" href="/styles.css"/>
</head>
<body class="layout">
  <header class="site-header">
    <div class="brand">
      <a href="/"><%= site?.title || 'My Portfolio' %></a>
    </div>
    <nav>
      <% (nav||[]).forEach(n=>{ %>
        <a href="<%= n.href %>" class="<%= (request?.url===n.href) ? 'active':'' %>"><%= n.text %></a>
      <% }) %>
    </nav>
  </header>
  <main class="site-main">
