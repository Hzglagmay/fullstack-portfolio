<%- include('partials/head') %>
<section class="card galaxy">
  <div class="crawl-track">
    <div class="crawl-content">
      <% site.hero.crawl.forEach(line => { %>
        <p><%= line %></p>
      <% }) %>
    </div>
  </div>
  <div class="note"><a href="/" class="btn">← Back</a></div>
</section>
<%- include('partials/footer') %>