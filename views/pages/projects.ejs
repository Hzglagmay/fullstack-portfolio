<%- include('../partials/head') %>
<div class="shell">
  <section class="page-header">
    <h1>My Projects</h1>
    <p>A collection of academic projects, personal experiments, and upcoming developments.</p>
  </section>

  <section class="projects-grid">
  <% (projects||[]).forEach(function(p){ %>
    <article class="proj card <%= p.soon?'soon':'' %>">
      <div class="icon">
        <% if(p.icon==='computer'){ %>💻<% } else if(p.icon==='github'){ %>⚡<% } else if(p.icon==='doc'){ %>📊<% } else { %>➕<% } %>
      </div>
      <h3><%= p.title %></h3>
      <% if(p.year){ %><div class="year"><%= p.year %></div><% } %>
      <% if(p.soon){ %><p style="color: var(--muted); margin: 4px 0;">In development...</p><% } %>
      <div class="actions">
        <% if(p.soon){ %>
          <span class="btn coming-soon">Coming Soon</span>
        <% } else { %>
          <a class="btn" href="<%= p.cta.href %>" target="_blank"><%= p.cta.text %></a>
        <% } %>
      </div>
    </article>
  <% }) %>
  </section>
</div>
<%- include('../partials/foot') %>
